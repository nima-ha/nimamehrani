/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/nimamehrani",
  output: "export",
  reactStrictMode: true,
  images: {
    unoptimized: true
  }
};

export default nextConfig;
